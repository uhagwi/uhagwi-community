# ──────────────────────────────────────────────
# 우하귀 웹앱 환경 변수 템플릿
# 사용법: `cp env.example.txt .env.local` 후 값 채우기
# 근거: docs/service-dev/02_design/api.md §1-3 인증 전략 · §3 봇 연동
# ──────────────────────────────────────────────

# ── NextAuth v5 ──────────────────────────────
# 로컬: http://localhost:3000, 배포: https://uhagwi.com
NEXTAUTH_URL=http://localhost:3000
# 임의 32바이트 이상 랜덤 문자열 (openssl rand -base64 32)
NEXTAUTH_SECRET=

# ── Discord OAuth (scope: identify email guilds.join) ──
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=

# ── Supabase ─────────────────────────────────
# 프로젝트 URL (https://xxxx.supabase.co)
SUPABASE_URL=
# 클라이언트 공개 키 (브라우저 노출 OK, RLS 적용)
SUPABASE_ANON_KEY=
# 서비스 롤 키 (서버 전용, 절대 클라이언트 노출 금지)
SUPABASE_SERVICE_ROLE_KEY=

# ── Upstash Redis (Rate Limit, idempotency) ──
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# ── Discord Bot ↔ 웹 HMAC 공유 시크릿 ───────────
# 봇 → 웹 webhook(X-Bot-Signature) 검증용 (api.md §3-2)
DISCORD_BOT_WEBHOOK_SECRET=
